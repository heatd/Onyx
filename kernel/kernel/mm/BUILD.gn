import("//build/kernel/kernel_sources.gni")
kernel_source_set("mm") {
  sources = [
    "bootmem.cpp",
    "flush.cpp",
    "page.cpp",
    "pagealloc.cpp",
    "vm.cpp",
    "vm_active.cpp",
    "vm_object.cpp",
  ]

  public_deps = [ "malloc" ]

  if (CONFIG_KASAN) {
    deps += [ "asan" ]
  }
}
