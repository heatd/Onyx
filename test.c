#include <stdlib.h>
#include <stdio.h>
int main()
{
	printf("C program compiled with newlib says hi");
	printf
	    ("\nI still need to support:\n -malloc()\n -scanf()\n -fopen() and stuff like that\n -fork() and exec()\n");
	while (1);
}
